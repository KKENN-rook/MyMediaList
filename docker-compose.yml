services:
  web:
    build: .
    ports:
      - "5000:5000"
    env_file:
      - .env
    volumes:
      # Mount instance directory to persist SQLite database outside the container
      - ./instance:/app/instance